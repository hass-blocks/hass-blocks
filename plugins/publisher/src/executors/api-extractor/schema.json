{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "ApiExtractor executor",
  "description": "",
  "type": "object",
  "properties": {
    "replacePaths": {
      "type": "boolean",
      "description": "Whether or not to replace tsconfig paths before generating rollup"
    },
    "strictChecks": {
      "type": "boolean",
      "description": "Whether or not to check for missing documentation and exports"
    },
    "mainEntrypointFile": {
      "type": "string",
      "description": "The main entrypoint for your package API"
    },
    "projectFolder": {
      "type": "string",
      "description": "Folder containing the package"
    },
    "reportFolder": {
      "type": "string",
      "description": "Folder to put the API report in"
    }
  },
  "required": []
}
